<%= simple_form_for [@team, @member] do |f| %>
  <%= f.input :name %>
  <%= f.input :positions, collection: @positions %>
  <%= f.input :phone %>
  <%= f.input :email %>
  <%= f.submit 'Save', class: 'btn btn-primary' %>
<% end %>
