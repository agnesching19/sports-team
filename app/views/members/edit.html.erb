<div class="container">
  <h1>Edit [ <%= @member.name.upcase %> ]</h1>
  <%= render 'form' %>
  <%= link_to 'Back', team_path(@team) %>
</div>

<%= content_for(:title, 'TEAM UP | Edit ' + @member.name) %>
